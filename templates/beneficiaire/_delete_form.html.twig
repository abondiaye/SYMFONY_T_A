<form method="post" action="{{ path('app_beneficiaire_delete', {'id': beneficiaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beneficiaire.id) }}">
    <button class="btn">Delete</button>
</form>
