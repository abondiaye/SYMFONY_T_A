<form method="post" action="{{ path('app_don_delete', {'id': don.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ don.id) }}">
    <button class="btn">Delete</button>
</form>
